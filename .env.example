# ── Mindmint Frontend ──────────────────────────────────────────────────
# Copy this file to .env and fill in the values.
#
# Any variable marked "optional" can be left empty or omitted; the
# matching feature will be hidden or fall back to mock data.

# ── Backend API ───────────────────────────────────────────────────────
# Base URL of the Mindmint REST API (no trailing slash).
# Auth, dashboard, and leaderboard calls all use this URL.
# When unset, sign-in fails closed with a configuration error; the
# dashboard activity feed and leaderboard fall back to bundled mock
# fixtures in dev.
VITE_BACKEND_API_URL=http://localhost:3000

# ── Google OAuth ──────────────────────────────────────────────────────
# OAuth 2.0 client id for "Sign in with Google".
# When unset, the Google button is not rendered and password sign-in
# remains available as the only auth method.
VITE_GOOGLE_CLIENT_ID=

# ── Contributors carousel ─────────────────────────────────────────────
# GitHub repo in owner/repo format used by the contributors section on
# the landing page.  Defaults to Riddlrealm/Mindmint-Frontend when
# omitted.
VITE_CONTRIBUTORS_REPO=Riddlrealm/Mindmint-Frontend
